#version 460

//layout (depth_less) out float gl_FragDepth;

void main()
{
    //gl_FragDepth = 0.5;//gl_FragCoord.x / 2048.0;// * gl_FragCoord.y / (512.0 * 512.0);
    //gl_FragDepth = 0.5f;
    //gl_FragDepth = gl_FragCoord.z;
}
